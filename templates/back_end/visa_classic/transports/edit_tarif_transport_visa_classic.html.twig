
 <style>
   .modal{
       margin-top: 35px;
       max-width: 1100px;
   } 
 </style>

{{ form_start(form, {'action': path('edit_tarif_transport_visa_classic', { 'id': transport.id}), 'method': 'POST'})}}

<div class="row">
    <h5 class=" m-auto col-8"><i class="fa fa-globe"></i>  {% if app.request.attributes.get('_route') == 'edit_tarif_transport_visa_classic' %} Modification de type de tarif / {{transport.titre}} {% else %} Ajout de type de tarif {% endif %}   </h5>
</div>

<div class="row justify-content-center mt-5">
    <div class="col-10 text-center">    
                
            <div class="row form-group text-right">
                {{ form_label(form.zone, 'Zone d\'application tarif', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.zone) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.departement, 'Département', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.departement) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.prix, 'Prix', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    <div class="input-group-prepend">
                        <span class="input-group-text">€ </span>
                    </div>
                    {{ form_widget(form.prix) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.actif, 'Actif', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-1">
                    {{ form_widget(form.actif) }}
                </div>
            </div>
            <button type="submit" class="btn btn-enregistrer"><i class="fas fa-save"></i> Enregistrer </a>
            {{form_widget(form._token)}}                

        {{ form_end(form, {'render_rest': false})}}
    </div>
</div>