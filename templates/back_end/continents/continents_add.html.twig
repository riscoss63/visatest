 <style>
   .modal{
       margin-top: 35px;
       max-width: 1100px;
   } 
 </style>

{{ form_start(form, {'action': path('add_continent'), 'method': 'POST'})}}

<div class="row">
    <h5 class=" m-auto col-8"><i class="fa fa-globe"></i>  Ajout de continent </h5>
</div>

<div class="row justify-content-center mt-5">
    <div class="col-10 text-center">    
                
            <div class="row form-group text-right">
                {{ form_label(form.titre, 'Libelle', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.titre) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.zonesGeographique, 'Zones géographique', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.zonesGeographique) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.active, 'Actif', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.active) }}
                </div>
            </div>
            <button type="submit" class="btn btn-enregistrer"><i class="fas fa-save"></i> Enregistrer </a>
            {{form_widget(form._token)}}
        {{ form_end(form, {'render_rest': false})}}
    </div>
</div>

<script>
    $('.my-select').selectpicker();
</script>
