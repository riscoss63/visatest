 <style>
   .modal{
       margin-top: 35px;
       max-width: 1100px;
       overflow: auto;
   } 
 </style>

{% if app.request.attributes.get('_route') == 'add_question_reponse' %} 
    {{ form_start(form, {'action': path('add_question_reponse', { 'sujettitre': id}), 'method': 'POST'})}}
{% else %} 
    {{ form_start(form, {'action': path('edit_question_reponse', { 'id': id}), 'method': 'POST'})}}
{% endif %}

<div class="row">
    <h5 class=" m-auto col-8"><i class="fa fa-globe"></i> 
{% if app.request.attributes.get('_route') == 'add_question_reponse' %} 
    Ajouter une question / réponse
{% else %} 
    Modification question / réponse
{% endif %} </h5>
</div>

<div class="row justify-content-center mt-5">
    <div class="col-10 text-center">    
            <div class="row form-group text-right">
                {{ form_label(form.sujetFaq, 'Sujet', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.sujetFaq) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.question, 'Question', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.question) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.reponse, 'Réponse', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.reponse) }}
                </div>
            </div>
            <button type="submit" class="btn btn-enregistrer"><i class="fas fa-save"></i> Enregistrer </a>
            {{ form_widget(form._token)}}
        {{ form_end(form, {'render_rest': false})}}
    </div>
</div>