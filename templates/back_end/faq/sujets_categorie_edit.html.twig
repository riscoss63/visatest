 <style>
   .modal{
       margin-top: 35px;
       max-width: 1100px;
   } 
 </style>

{{ form_start(form, {'action': path('add_sujet_categorie_faq', { 'id': id}), 'method': 'POST'})}}

<div class="row">
    <h5 class=" m-auto col-8"><i class="fa fa-globe"></i>  Ajout de sujet </h5>
</div>

<div class="row justify-content-center mt-5">
    <div class="col-10 text-center">    
                
            <div class="row form-group text-right">
                {{ form_label(form.categorieFaq, 'Catégorie', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.categorieFaq) }}
                </div>
            </div>
            <div class="row form-group text-right">
                {{ form_label(form.titre, 'Sujet', {'label_attr' : {'class': 'col-sm-2 col-form-label'}}) }}
                <div class="input-group col-sm-10">
                    {{ form_widget(form.titre) }}
                </div>
            </div>
            <button type="submit" class="btn btn-enregistrer"><i class="fas fa-save"></i> Enregistrer </a>

        {{ form_end(form, {'render_rest': false})}}
    </div>
</div>