{% extends "back_end/base.html.twig" %}

{% block menu_header %}
    {% include "back_end/faq/__header.html.twig" %}
{% endblock %}

{% block contenu %}

    <div class="row">
        <h5 class=" m-auto col-8"><i class="fa fa-users-cog"></i> Gestion des questions/réponses</h5>
        <div class="col-3 float-right">
            <a href="{{path('add_question_reponse')}}" class="btn btn-primary petite-taille"><i class="fas fa-plus-circle"></i> Ajouter une question/reponse </a>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <table id="table" data-toggle="table" data-url="{{ path('json_questions_reponses')}}" data-pagination="true" data-search="true" data-pagination: "true">
                <thead>

                    <tr>
                        <th data-field="sujetFaq.categorieFaq.titre" data-sortable="true"> Catégorie </th>
                        <th data-field="sujetFaq.titre" data-sortable="true"> Sujet </th>
                        <th data-field="question" data-align="center" data-sortable="true">Question</th>
                        <th data-field="reponse" data-sortable="true"> Réponse </th>
                        <th data-field="edit" data-formatter="actionsFormatter"> Editer </th>
                    </tr>

                </thead>
            </table>
        </div> 
    </div>

{% endblock %}

{% block javascripts %}

    {{ parent() }}
    <script>

         function actionsFormatter(value, row, index){
            return '<a href="'+Routing.generate('edit_question_reponse', {'id' : row.id})+'" title="Modifier"><i class="far fa-edit"></i></a>';
        }

        var $table = $('#table')
        var $button = $('select')

        $(function() {
            $button.change(function () {
                $( "select option:selected" ).each(function() {
                    $table.bootstrapTable('filterBy', {
                        typeDuree: $( this ).val()
                    })
                    console.log($( this ).val())
                });
                
            })
        })
        
        function booleanFormatter(value, row, index) {
            if (value === true) {
                return '<i class="fa fa-check text-success text-center"></i>';
            }
            else {
                return '<i class="fa fa-times text-danger text-center"></i>';
            }
        }

        function visaFormatter(value, row, index) {
            if(row["visaClassic"]) {
                return '<strong> Visa classic </strong>';
            }
            else if(row["evisa"]) {
                return '<strong> E visa </strong';
            }
            else if(row["carteTourisme"]) {
                return '<strong>Carte de tourisme</strong>';
            }
            return 'Aucun visa n\'est selectionner';
        }

        
        
    </script>
    
{% endblock %}